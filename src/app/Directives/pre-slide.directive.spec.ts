import { PreSlideDirective } from './pre-slide.directive';

describe('PreSlideDirective', () => {
  it('should create an instance', () => {
    const directive = new PreSlideDirective();
    expect(directive).toBeTruthy();
  });
});
